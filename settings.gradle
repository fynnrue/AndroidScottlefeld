pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven {
            url "https://mvn.cit-ec.de/nexus/content/repositories/releases/"
        }
    }
}
rootProject.name = "ProjectTemplate"
include ':app'
